<h1>プランを決める</h1>

 <%= form_with model: @plan, local:true do |f| %>
    <div class="form-group">
      <%= f.label :start_weight, "開始体重" %>
      <%= f.text_field :start_weight %>
    </div>

    <div class="form-group">
      <%= f.label :target_weight, "目標体重" %>
      <%= f.text_field :target_weight %>
    </div>
    <div class="actions">
      <%= f.submit "プラン決定！", class:"btn btn-secondary" %>
      <% end %>
    </div>